[tool.poetry]
name = "pipeline-vulnerabilities-threshold"
version = "0.0.1"
description = ""
authors = ["IA Interactive"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
opentelemetry-exporter-otlp-proto-http = "1.18.0"